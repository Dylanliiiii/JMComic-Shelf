# Application icons

`pyappify-action` reads optional application icons from this directory.

- `icon.png` is copied from `assets/icon.png`.
- `icon.ico` is generated from `assets/icon.png` for Windows packaging.

Replace both files when the final JMComic Shelf icon is ready.
